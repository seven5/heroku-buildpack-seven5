#!/usr/bin/env bash
# bin/detect <build-dir>
set -e

go="$(find "$1/go" -type f -name '*.go' | sed 1q)"
dart="$(find "$1/dart" -type f -name '*.dart' | sed 1q)"
if test -n "$go" -a -n "$dart"  
then echo Seven5
else echo no; exit 1
fi
